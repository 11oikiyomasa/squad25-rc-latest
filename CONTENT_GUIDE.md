# Content guide

Replace the placeholders before public launch:

- `public/images/members/*.svg` -> real 25 member photos, keeping the same filenames or updating `photo` in `data/squad.ts`.
- `name`, `nickname`, `role`, `hero`, `bio`, `status` in `data/squad.ts`.
- `youtubeId` for each montage.
- `achievements` in `data/squad.ts`.

Keep profile photos vertical (roughly 4:5) for best cropping.
